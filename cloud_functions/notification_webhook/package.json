{
  "name": "notification-webhook",
  "version": "1.0.0",
  "description": "Cloud Function to send email notifications via SMTP",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=sendNotification",
    "dev": "functions-framework --target=sendNotification --port 8080"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.0.0",
    "dotenv": "^16.0.0",
    "nodemailer": "^6.9.0"
  },
  "devDependencies": {
    "proxyquire": "^2.1.3"
  }
}
